const dialog = require('../../pageObjects/dialog.page');
const {expect} = require('chai').expect;

describe('App Test', ()=>{

        //run hook
        before(() => {
                
        });
        it('test', ()=>{

            
        })

})